spring:
  datasource:
    url: jdbc:h2:mem:vapex
    driver-class-name: org.h2.Driver
    username: sa
    password: password
    type:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true